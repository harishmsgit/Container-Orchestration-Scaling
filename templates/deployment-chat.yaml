apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.chat.name }}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: {{ .Values.services.chat.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.chat.name }}
    spec:
      containers:
        - name: {{ .Values.services.chat.name }}
          image: "{{ .Values.image.repository }}/{{ .Values.services.chat.name }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.chat.port }}
