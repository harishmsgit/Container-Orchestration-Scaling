apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.streaming.name }}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: {{ .Values.services.streaming.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.streaming.name }}
    spec:
      containers:
        - name: {{ .Values.services.streaming.name }}
          image: "{{ .Values.image.repository }}/{{ .Values.services.streaming.name }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.streaming.port }}
