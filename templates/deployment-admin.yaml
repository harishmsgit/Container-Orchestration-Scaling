apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.admin.name }}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: {{ .Values.services.admin.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.admin.name }}
    spec:
      containers:
        - name: {{ .Values.services.admin.name }}
          image: "{{ .Values.image.repository }}/{{ .Values.services.admin.name }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.admin.port }}
